const React = require('react');

const New = (props) => {
  return (
    <div>
      {/* inside here goes your jsx (html stuff)*/}
    </div>
  )
}

module.exports = New;

// Add a form (like in the fruits new page) to collect the data you need to make a new veggie